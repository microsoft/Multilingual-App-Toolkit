<Window x:Class="DocViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocViewer"
        xmlns:p="clr-namespace:DocViewer.Resources"


        mc:Ignorable="d"
        Title="{x:Static p:Resources.Title}" Height="475" Width="325">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10" Grid.Row="1" Grid.Column="1"/>


        <TextBlock Grid.Row="1" Grid.Column="0" TextWrapping="Wrap"  Text="{x:Static p:Resources.Para}">
        </TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold" FontStyle="Italic" Text="{x:Static p:Resources.Title2}">
        </TextBlock>


    </Grid>

</Window>
